experiment:
  name: resnet18_pretrained_lr_low

model:
  name: resnet18
  pretrained: true
  num_classes: 37

training:
  epochs: 20
  optimizer: adamw
  backbone_lr: 0.00005
  head_lr: 0.0005
  weight_decay: 0.0001
  scheduler: cosine
  min_lr: 0.000001
  amp: true

logging:
  backend: none
