experiment:
  name: resnet18_pretrained_lr_high

model:
  name: resnet18
  pretrained: true
  num_classes: 37

training:
  epochs: 20
  optimizer: adamw
  backbone_lr: 0.0003
  head_lr: 0.003
  weight_decay: 0.0001
  scheduler: cosine
  min_lr: 0.000001
  amp: true

logging:
  backend: none
