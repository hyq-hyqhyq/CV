experiment:
  name: resnet18_pretrained_30epochs

model:
  name: resnet18
  pretrained: true
  num_classes: 37

training:
  epochs: 30
  optimizer: adamw
  backbone_lr: 0.0001
  head_lr: 0.001
  weight_decay: 0.0001
  scheduler: cosine
  min_lr: 0.000001
  amp: true

logging:
  backend: none
